## 2024-05-22 - Accessibility Quick Wins
**Learning:** Adding `aria-label` and basic focus management (using a global `lastFocusedElement`) provides a massive accessibility boost with very few lines of code (< 20 lines) in a vanilla JS context.
**Action:** Always check icon-only buttons for labels and ensure modals restore focus to their trigger element.
